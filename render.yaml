services:
  - type: web
    name: world-weather-tracker
    runtime: static
    buildCommand: echo "No build step required for this static site"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
